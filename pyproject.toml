[project]
name = "envoy-grpc-json"
version = "0.1.0"
description = "Simple gRPC server with Envoy grpc-json transcoding"
requires-python = ">=3.11"
dependencies = [
    "grpcio>=1.60.0",
    "grpcio-tools>=1.60.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
